attractor_runs/
smoke_test_run_dir/
bin/
.nrepl-port
*.log
.env
!.env.sample
.worktrees/

# Keep project plans and specifications tracked despite global ignores.
!docs/superpowers/
!docs/superpowers/**

# local provider registry (see attractor.edn.sample)
attractor.edn

# private notes, never committed
docs/notes.md
